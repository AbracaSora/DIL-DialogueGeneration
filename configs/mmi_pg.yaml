extends: "configs/base.yaml"
experiment_name: "mmi_pg"

mmi:
  backward_model_ckpt: "outputs/seq2seq_backward/best.pt"
  num_samples: 5
  curriculum_start_ratio: 1.0
  curriculum_end_ratio: 0.0
  baseline_hidden_dim: 256

train:
  epochs: 4
  batch_size: 32
  lr: 0.0002
